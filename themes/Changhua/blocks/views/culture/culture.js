// var pic_url = '';
// var a='';
// $(function(){	  
	  // var postdata={
	  // 'nType':'3',
	  // 'pri':'2'
	  // };
      // var req = {url: 'changhua/changhua/culture/list',post:postdata, hasCA: true};
    // __.api(req, function(data) {
      // if (data.errCode === 0) {	  
		// data.value.list.forEach(function(item) {
			// get_pic(item.ngID,item.title,item.iconURI);
		// })	
				
      // } else {
	  // alert(data.message);
      // }
    // });
	
// });
// function get_pic(ngID,title,icon){
		// a='';
		// a+="<div class='item'><a href='culture_buy/"+ngID+"?ngID="+ngID+"#page_top'><div class='item_pic'><img src='/images/"+ngID+"?path=";
		// a+=icon;
		// a+="&maxw=145'></div><div class='item_title'>";
		// a+=title;
		// a+="</div></a></div>";
		// document.getElementById('content').innerHTML+=a;
// };